import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { colors, spacing } from '@/theme';
const groups = ['AI Intelligence', 'Track', 'CopyTrade', 'Top Traders', 'Smart Money', 'Wallet Tracker', 'Signals', 'Heatmap', 'Snipe List', 'Analytics', 'Multicharts', 'Feed Data', 'Claim Monitor'];
export default function MoreScreen() { return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Text style={styles.eyebrow}>TERMINAL DEX</Text><Text style={styles.title}>More tools</Text><Text style={styles.note}>Routes are listed now to preserve the approved information architecture; each activates only after its real contract and security checks are implemented.</Text><View style={styles.list}>{groups.map((item) => <View key={item} style={styles.row}><Text style={styles.label}>{item}</Text><Text style={styles.status}>QUEUED</Text></View>)}</View></ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, content: { padding: spacing.lg }, eyebrow: { color: colors.accent, fontSize: 10, fontWeight: '900', letterSpacing: 2 }, title: { color: colors.text, fontSize: 28, fontWeight: '900', marginTop: 4 }, note: { color: colors.muted, lineHeight: 20, marginVertical: spacing.xl }, list: { borderWidth: 1, borderColor: colors.border, borderRadius: 16, overflow: 'hidden' }, row: { minHeight: 54, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: spacing.lg, backgroundColor: colors.surface, borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: colors.border }, label: { color: colors.text, fontWeight: '700' }, status: { color: colors.muted, fontSize: 9, fontWeight: '900', letterSpacing: 1 } });
