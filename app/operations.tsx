import { Ionicons } from '@expo/vector-icons';
import { useQuery } from '@tanstack/react-query';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { useMemo, useState } from 'react';
import { ActivityIndicator, Pressable, RefreshControl, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { fetchDiscovery, fetchFeedConnections, fetchFeedDiagnostics, fetchTopTraders } from '@/api/client';
import type { FeedConnectionsResponse, MarketToken } from '@/api/schema';
import { compactUsd, signedPercent, tokenPrice } from '@/lib/format';
import { isSolanaAddress } from '@/security/input';
import { colors, spacing } from '@/theme';

type Tab = 'analytics' | 'feed';
const emptyFilters = { dex: 'All', minLiquidity: '', minMarketCap: '' };

export default function OperationsScreen() {
  const params = useLocalSearchParams<{ tab?: string }>(); const router = useRouter();
  const [tab, setTab] = useState<Tab>(params.tab === 'feed' ? 'feed' : 'analytics');
  const market = useQuery({ queryKey: ['operations-market'], queryFn: ({ signal }) => fetchDiscovery('trending', '24h', emptyFilters, undefined, signal), enabled: tab === 'analytics', refetchInterval: 30_000 });
  const gainers = useQuery({ queryKey: ['operations-gainers'], queryFn: ({ signal }) => fetchDiscovery('gainers', '1h', emptyFilters, undefined, signal), enabled: tab === 'analytics', refetchInterval: 30_000 });
  const fresh = useQuery({ queryKey: ['operations-new-pairs'], queryFn: ({ signal }) => fetchDiscovery('new-pairs', '24h', emptyFilters, undefined, signal), enabled: tab === 'analytics', refetchInterval: 30_000 });
  const traders = useQuery({ queryKey: ['operations-traders'], queryFn: ({ signal }) => fetchTopTraders('30D', signal), enabled: tab === 'analytics', refetchInterval: 60_000 });
  const connections = useQuery({ queryKey: ['feed-connections'], queryFn: ({ signal }) => fetchFeedConnections(signal), enabled: tab === 'feed', refetchInterval: 30_000, retry: 1 });
  const diagnostics = useQuery({ queryKey: ['feed-diagnostics'], queryFn: ({ signal }) => fetchFeedDiagnostics(signal), enabled: tab === 'feed', refetchInterval: 30_000, retry: 1 });
  const refreshing = [market, gainers, fresh, traders, connections, diagnostics].some((query) => query.isRefetching);
  const refresh = () => tab === 'analytics' ? Promise.all([market.refetch(), gainers.refetch(), fresh.refetch(), traders.refetch()]) : Promise.all([connections.refetch(), diagnostics.refetch()]);
  return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content} refreshControl={<RefreshControl refreshing={refreshing} onRefresh={refresh} tintColor={colors.accent} />}>
    <View style={styles.header}><Pressable accessibilityRole="button" accessibilityLabel="Back from Operations" onPress={() => router.back()} style={styles.back}><Ionicons name="arrow-back" size={18} color={colors.text} /></Pressable><View style={styles.flex}><Text style={styles.eyebrow}>READ-ONLY OPERATIONAL EVIDENCE</Text><Text style={styles.title}>Analytics & Feeds</Text></View><Text style={styles.readOnly}>GET ONLY</Text></View>
    <View style={styles.notice}><Ionicons name="shield-checkmark" size={15} color={colors.warning} /><Text style={styles.noticeText}>Market analytics summarize observed provider records. Feed Data observes runtime and persistence evidence; it never activates probes, changes providers, replays events, or exposes credentials.</Text></View>
    <View accessibilityRole="tablist" style={styles.tabs}>{(['analytics', 'feed'] as const).map((item) => <Pressable key={item} accessibilityRole="tab" accessibilityState={{ selected: tab === item }} onPress={() => setTab(item)} style={[styles.tab, tab === item && styles.tabActive]}><Text style={[styles.tabText, tab === item && styles.tabTextActive]}>{item === 'analytics' ? 'Analytics' : 'Feed Data'}</Text></Pressable>)}</View>
    {tab === 'analytics' ? <AnalyticsPanel market={market} gainers={gainers} fresh={fresh} traders={traders} onToken={(address) => router.push({ pathname: '/token/[address]', params: { address } })} /> : <FeedPanel connections={connections} diagnostics={diagnostics} />}
  </ScrollView></SafeAreaView>;
}

type MarketQuery = ReturnType<typeof useQuery<Awaited<ReturnType<typeof fetchDiscovery>>, Error>>;
type TraderQuery = ReturnType<typeof useQuery<Awaited<ReturnType<typeof fetchTopTraders>>, Error>>;
function AnalyticsPanel({ market, gainers, fresh, traders, onToken }: { market: MarketQuery; gainers: MarketQuery; fresh: MarketQuery; traders: TraderQuery; onToken: (address: string) => void }) {
  const usable = useMemo(() => (market.data?.tokens ?? []).filter((token) => isSolanaAddress(token.address)), [market.data]);
  const excluded = (market.data?.tokens.length ?? 0) - usable.length;
  const summary = useMemo(() => summarize(usable), [usable]);
  const leaders = (gainers.data?.tokens ?? []).filter((token) => isSolanaAddress(token.address)).slice(0, 6);
  const newPairs = [...new Map((fresh.data?.tokens ?? []).filter((token) => isSolanaAddress(token.address)).map((token) => [token.address, token])).values()].slice(0, 5);
  if (market.isLoading) return <State loading text="Loading live Solana analytics…" />;
  if (market.error && !usable.length) return <State text={market.error.message} />;
  return <View>
    <EvidenceBar warning={market.data?.freshness?.isStale === true || Boolean(market.data?.error)} text={`${market.data?.source ?? 'source unavailable'} · ${market.data?.dataQuality ?? 'quality unavailable'} · ${usable.length} exact-mint records · ${excluded} excluded`} />
    <View style={styles.kpis}><Kpi label="Tracked" value={String(summary.count)} /><Kpi label="Gainers" value={`${summary.positivePct.toFixed(1)}%`} /><Kpi label="Liquidity" value={compactUsd(summary.liquidity)} /><Kpi label="1H volume" value={compactUsd(summary.volume)} /></View>
    <SectionTitle title="Momentum leaders" detail="Observed 1-hour change; not a prediction" />
    {gainers.error ? <InlineWarning text={gainers.error.message} /> : leaders.map((token) => <MarketRow key={token.address} token={token} onOpen={() => onToken(token.address)} />)}
    {!leaders.length && !gainers.error ? <Empty text="No exact-mint momentum records are available." /> : null}
    <SectionTitle title="Fresh pairs" detail="Deduplicated by canonical mint" />
    {fresh.error ? <InlineWarning text={fresh.error.message} /> : newPairs.map((token) => <MarketRow key={token.address} token={token} onOpen={() => onToken(token.address)} />)}
    {!newPairs.length && !fresh.error ? <Empty text="No exact-mint fresh pairs are available." /> : null}
    <SectionTitle title="Historical trader activity" detail={`${traders.data?.source ?? 'source unavailable'} · 30-day observation`} />
    {traders.error ? <InlineWarning text={traders.error.message} /> : (traders.data?.traders ?? []).slice(0, 5).map((trader) => <View key={trader.address} style={styles.trader}><Text style={styles.rank}>#{trader.rank}</Text><View style={styles.flex}><Text style={styles.rowTitle}>{trader.address.slice(0, 5)}…{trader.address.slice(-4)}</Text><Text style={styles.meta}>{trader.badge} · {trader.trades} indexed trades · {trader.winRate.toFixed(1)}% historical win rate</Text></View><Text style={[styles.metric, trader.pnlUsd < 0 && styles.bad]}>{compactUsd(trader.pnlUsd)}</Text></View>)}
    <Text style={styles.boundary}>Aggregates use only canonical token identities returned by the backend. Missing transaction counts, cost basis, and predictive claims are not inferred.</Text>
  </View>;
}

type ConnectionsQuery = ReturnType<typeof useQuery<Awaited<ReturnType<typeof fetchFeedConnections>>, Error>>;
type DiagnosticsQuery = ReturnType<typeof useQuery<Awaited<ReturnType<typeof fetchFeedDiagnostics>>, Error>>;
function FeedPanel({ connections, diagnostics }: { connections: ConnectionsQuery; diagnostics: DiagnosticsQuery }) {
  if (connections.isLoading && diagnostics.isLoading) return <State loading text="Loading observational feed evidence…" />;
  const inventory = connections.data; const health = diagnostics.data;
  return <View>
    {inventory ? <EvidenceBar warning={inventory.healthSummary.degraded > 0 || inventory.healthSummary.unhealthy > 0} text={`${inventory.runtimeScope} · generated ${ageLabel(inventory.generatedAt)} · ${inventory.healthSummary.receiving} receiving`} /> : null}
    {connections.error ? <InlineWarning text={connections.error.message} /> : null}{diagnostics.error ? <InlineWarning text={diagnostics.error.message} /> : null}
    {inventory ? <View style={styles.kpis}><Kpi label="Healthy" value={String(inventory.healthSummary.healthy)} /><Kpi label="Degraded" value={String(inventory.healthSummary.degraded)} /><Kpi label="Unhealthy" value={String(inventory.healthSummary.unhealthy)} /><Kpi label="Receiving" value={String(inventory.healthSummary.receiving)} /></View> : null}
    {health ? <><SectionTitle title="Runtime quality" detail={`${health.runtimeScope} · ${health.degraded ? 'degraded evidence' : 'no reported degradation'}`} /><View style={styles.quality}>{Object.entries(health.quality as Record<string, string>).map(([label, status]) => <View key={label} style={styles.qualityItem}><Text style={styles.kpiLabel}>{label.replace(/([A-Z])/g, ' $1')}</Text><Text style={[styles.qualityValue, !['healthy', 'receiving'].includes(status) && styles.warn]}>{status.replaceAll('_', ' ')}</Text></View>)}</View><View style={styles.evidenceStates}><Text style={styles.meta}>Persistence evidence: {health.persistenceEvidence.status}</Text><Text style={styles.meta}>Observability evidence: {health.observabilityEvidence.status}</Text><Text style={styles.meta}>Replay evidence: {health.replayEvidence.status}</Text><Text style={styles.meta}>Realtime timestamp: {health.realtimeEvidence.timestampValid ? health.realtimeEvidence.timestamp ?? 'valid, unavailable' : 'invalid / unavailable'}</Text></View>{health.actions.slice(0, 5).map((action) => <InlineWarning key={action} text={action} />)}</> : null}
    <SectionTitle title="Provider inventory" detail="Configuration is not proof of live delivery" />
    {inventory?.connections.map((item) => <FeedConnectionCard key={item.id} item={item} />)}
    {!inventory && !connections.error ? <Empty text="No feed inventory evidence is available." /> : null}
    <SectionTitle title="Recent ingestion jobs" detail="Durable job outcomes; no controls" />
    {inventory?.ingestionJobs.slice(0, 6).map((job) => <View key={job.id} style={styles.job}><View style={styles.flex}><Text style={styles.rowTitle}>{job.jobType}</Text><Text style={styles.meta}>{job.providerLabel} · {String(job.startedAt)}</Text></View><View><Text style={[styles.status, job.status !== 'completed' && styles.warn]}>{job.status}</Text><Text style={styles.meta}>{job.tokensProcessed} ok · {job.tokensFailed} failed</Text></View></View>)}
    {!inventory?.ingestionJobs.length ? <Empty text="No durable ingestion jobs were returned." /> : null}
    <Text style={styles.boundary}>Read-only summary. Endpoint URLs, sample records, provider keys, active probes, replay controls, and scheduler mutations are intentionally absent.</Text>
  </View>;
}

export function FeedConnectionCard({ item }: { item: FeedConnectionsResponse['connections'][number] }) { const warning = item.health === 'degraded' || item.health === 'unhealthy' || item.records.freshness === 'stale'; return <View accessibilityRole="summary" accessibilityLabel={`${item.label}, ${item.health}, ${item.records.total} persisted records`} style={styles.card}><View style={styles.cardTop}><View style={[styles.dot, { backgroundColor: warning ? colors.warning : colors.positive }]} /><View style={styles.flex}><Text style={styles.rowTitle}>{item.label}</Text><Text style={styles.meta}>{item.method.toUpperCase()} · {item.deliveryStatus.replaceAll('_', ' ')} · {item.configured ? 'configured' : 'not configured'}</Text></View><Text style={[styles.status, warning && styles.warn]}>{item.health}</Text></View><View style={styles.recordLine}><Text style={styles.meta}>{item.records.pairs.toLocaleString()} pairs</Text><Text style={styles.meta}>{item.records.transactions.toLocaleString()} txns</Text><Text style={styles.meta}>{item.records.candles.toLocaleString()} candles</Text><Text style={styles.metric}>{item.records.total.toLocaleString()}</Text></View><Text style={styles.freshness}>{item.records.freshness ?? 'freshness unavailable'} · {item.records.lastPersistedAt ? ageLabel(item.records.lastPersistedAt) : 'no persisted observation'}</Text></View>; }

function summarize(tokens: MarketToken[]) { const count = tokens.length; return { count, positivePct: tokens.filter((token) => token.change1h > 0).length / Math.max(count, 1) * 100, liquidity: tokens.reduce((sum, token) => sum + Math.max(0, token.liquidity), 0), volume: tokens.reduce((sum, token) => sum + Math.max(0, token.volume1h), 0) }; }
function ageLabel(timestamp: number) { if (!Number.isFinite(timestamp)) return 'time unavailable'; const seconds = Math.max(0, Math.floor((Date.now() - timestamp) / 1000)); return seconds < 60 ? `${seconds}s ago` : seconds < 3600 ? `${Math.floor(seconds / 60)}m ago` : `${Math.floor(seconds / 3600)}h ago`; }
function MarketRow({ token, onOpen }: { token: MarketToken; onOpen: () => void }) { return <Pressable accessibilityRole="button" accessibilityLabel={`Open ${token.symbol} analytics`} onPress={onOpen} style={styles.marketRow}><View style={styles.flex}><Text style={styles.rowTitle}>{token.symbol} · {token.name}</Text><Text style={styles.meta}>LIQ {compactUsd(token.liquidity)} · VOL {compactUsd(token.volume1h)} · {token.dex}</Text></View><View><Text style={styles.metric}>{tokenPrice(token.price)}</Text><Text style={[styles.change, token.change1h < 0 && styles.bad]}>{signedPercent(token.change1h)}</Text></View></Pressable>; }
function EvidenceBar({ warning, text }: { warning: boolean; text: string }) { return <View style={styles.bar}><Ionicons name={warning ? 'warning' : 'checkmark-circle'} size={13} color={warning ? colors.warning : colors.positive} /><Text style={styles.barText}>{text}</Text><Text style={[styles.status, warning && styles.warn]}>{warning ? 'CHECK' : 'CURRENT'}</Text></View>; }
function Kpi({ label, value }: { label: string; value: string }) { return <View style={styles.kpi}><Text style={styles.kpiLabel}>{label}</Text><Text numberOfLines={1} style={styles.kpiValue}>{value}</Text></View>; }
function SectionTitle({ title, detail }: { title: string; detail: string }) { return <View style={styles.section}><Text style={styles.sectionTitle}>{title}</Text><Text style={styles.meta}>{detail}</Text></View>; }
function InlineWarning({ text }: { text: string }) { return <View accessibilityRole="alert" style={styles.warningBox}><Text style={styles.warningText}>{text}</Text></View>; }
function Empty({ text }: { text: string }) { return <View style={styles.empty}><Text style={styles.meta}>{text}</Text></View>; }
function State({ loading, text }: { loading?: boolean; text: string }) { return <View style={styles.state}>{loading ? <ActivityIndicator color={colors.accent} /> : <Ionicons name="information-circle" size={20} color={colors.muted} />}<Text style={styles.stateText}>{text}</Text></View>; }

const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, content: { paddingBottom: 90 }, header: { padding: spacing.lg, flexDirection: 'row', alignItems: 'center', gap: spacing.md }, back: { width: 36, height: 36, alignItems: 'center', justifyContent: 'center', borderWidth: 1, borderColor: colors.border, borderRadius: 10 }, flex: { flex: 1 }, eyebrow: { color: '#72c8ff', fontSize: 8, fontWeight: '900', letterSpacing: 1.5 }, title: { color: colors.text, fontSize: 23, fontWeight: '900' }, readOnly: { color: colors.warning, fontSize: 7, fontWeight: '900', borderWidth: 1, borderColor: '#5a4720', borderRadius: 7, padding: 6 }, notice: { marginHorizontal: spacing.lg, padding: spacing.md, flexDirection: 'row', gap: spacing.sm, borderRadius: 11, borderWidth: 1, borderColor: '#5a4720', backgroundColor: '#241d10' }, noticeText: { flex: 1, color: colors.warning, fontSize: 9, lineHeight: 14 }, tabs: { margin: spacing.lg, flexDirection: 'row', padding: 4, borderRadius: 12, backgroundColor: colors.surface }, tab: { flex: 1, alignItems: 'center', paddingVertical: 10, borderRadius: 9 }, tabActive: { backgroundColor: colors.accentDim }, tabText: { color: colors.muted, fontSize: 10, fontWeight: '800' }, tabTextActive: { color: colors.accent }, bar: { marginHorizontal: spacing.lg, marginBottom: spacing.md, padding: spacing.sm, flexDirection: 'row', alignItems: 'center', gap: 6, borderWidth: 1, borderColor: colors.border, borderRadius: 9, backgroundColor: colors.surface }, barText: { flex: 1, color: colors.muted, fontSize: 8 }, kpis: { paddingHorizontal: spacing.lg, flexDirection: 'row', flexWrap: 'wrap', gap: spacing.sm, marginBottom: spacing.md }, kpi: { flexGrow: 1, minWidth: '21%', padding: spacing.md, borderWidth: 1, borderColor: colors.border, borderRadius: 10, backgroundColor: colors.surface }, kpiLabel: { color: colors.muted, fontSize: 7, textTransform: 'uppercase' }, kpiValue: { color: colors.text, fontSize: 13, fontWeight: '900', marginTop: 4 }, section: { marginTop: spacing.md, marginHorizontal: spacing.lg, marginBottom: spacing.sm }, sectionTitle: { color: colors.text, fontSize: 12, fontWeight: '900' }, marketRow: { minHeight: 62, marginHorizontal: spacing.lg, marginBottom: 2, padding: spacing.md, flexDirection: 'row', alignItems: 'center', gap: spacing.md, borderWidth: 1, borderColor: colors.border, borderRadius: 10, backgroundColor: colors.surface }, rowTitle: { color: colors.text, fontSize: 10, fontWeight: '900' }, meta: { color: colors.muted, fontSize: 8, lineHeight: 12, marginTop: 2 }, metric: { color: colors.text, fontSize: 10, fontWeight: '900', textAlign: 'right' }, change: { color: colors.positive, fontSize: 8, textAlign: 'right', marginTop: 2 }, bad: { color: colors.negative }, trader: { marginHorizontal: spacing.lg, marginBottom: 2, padding: spacing.md, flexDirection: 'row', alignItems: 'center', gap: spacing.sm, borderWidth: 1, borderColor: colors.border, borderRadius: 10, backgroundColor: colors.surface }, rank: { width: 24, color: colors.muted, fontSize: 9, fontWeight: '900' }, boundary: { color: colors.muted, fontSize: 8, lineHeight: 13, textAlign: 'center', margin: spacing.xl }, warningBox: { marginHorizontal: spacing.lg, marginBottom: spacing.sm, padding: spacing.sm, borderWidth: 1, borderColor: '#5a4720', borderRadius: 9, backgroundColor: '#241d10' }, warningText: { color: colors.warning, fontSize: 8, lineHeight: 12 }, empty: { marginHorizontal: spacing.lg, padding: spacing.lg, alignItems: 'center', borderWidth: 1, borderColor: colors.border, borderRadius: 10 }, quality: { paddingHorizontal: spacing.lg, flexDirection: 'row', flexWrap: 'wrap', gap: spacing.sm }, qualityItem: { width: '48%', padding: spacing.md, borderWidth: 1, borderColor: colors.border, borderRadius: 10, backgroundColor: colors.surface }, qualityValue: { color: colors.positive, fontSize: 10, fontWeight: '900', marginTop: 4, textTransform: 'uppercase' }, warn: { color: colors.warning }, evidenceStates: { margin: spacing.lg, padding: spacing.md, borderWidth: 1, borderColor: colors.border, borderRadius: 10, backgroundColor: colors.surface }, card: { marginHorizontal: spacing.lg, marginBottom: spacing.sm, padding: spacing.md, borderWidth: 1, borderColor: colors.border, borderRadius: 11, backgroundColor: colors.surface }, cardTop: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm }, dot: { width: 7, height: 7, borderRadius: 4 }, status: { color: colors.positive, fontSize: 7, fontWeight: '900', textTransform: 'uppercase' }, recordLine: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginTop: spacing.sm, paddingTop: spacing.sm, borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: colors.border }, freshness: { color: colors.muted, fontSize: 7, marginTop: 5 }, job: { marginHorizontal: spacing.lg, marginBottom: 2, padding: spacing.md, flexDirection: 'row', alignItems: 'center', gap: spacing.md, borderWidth: 1, borderColor: colors.border, borderRadius: 10, backgroundColor: colors.surface }, state: { minHeight: 220, alignItems: 'center', justifyContent: 'center', gap: spacing.md, padding: spacing.xl }, stateText: { color: colors.muted, textAlign: 'center', lineHeight: 18 } });
