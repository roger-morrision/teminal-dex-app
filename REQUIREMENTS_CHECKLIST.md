# Terminal DEX Mobile Requirements Checklist

Status legend: `[x]` verified, `[ ]` incomplete, `[~]` intentionally visible but backend-connected behavior incomplete.

## MOBILE-199 persistent verified runtime

- [x] Detached fixture/Metro processes retain immutable HEAD provenance after the launching shell exits.
- [x] Runtime state and logs are temp-only, ports are bounded and must be available, and shutdown requires matching project ownership.
- [x] Explicit start/status/stop commands exist; no unknown process or occupied port is terminated.
- [ ] QA must complete rendered scenario and device evidence on the persistent runtime.

## MOBILE-198 controlled QA evidence

- [x] QA-only provider fixture is opt-in and never selected by production code.
- [x] Current, empty, stale, offline, mixed-DEX, pagination, and retry states are deterministic.
- [x] Fixture control is bounded and contains no credentials or real provider payloads.
- [ ] QA must execute the exact-build runtime/device matrix before closing `MOBILE-QA-273`, `278`, `279`, or `287`.

## Fourteen-phase delivery ledger (MOBILE-523..527)

- [x] Repository-local Expo Doctor prepends the actual `process.execPath` directory and supplies a temporary, fail-closed npm compatibility command limited to version reporting and lockfile-backed dependency explanation, eliminating isolated Windows `spawn node/npm ENOENT`; the exact non-escalated QA-style lane passes 21/21 checks.
- [x] Verified Metro startup probes a bounded 19-port window from 8081 (or `MOBILE_DEV_PORT`) and selects the first free loopback port without terminating unknown processes; explicit `--port` remains authoritative.
- [x] Privy initialization is bounded to 12 seconds: a stalled SDK now becomes a localized, screen-reader-announced recovery state with a working return action instead of trapping the user behind an infinite spinner.
- [x] Verified development startup applies a bounded two-worker Metro default (configurable from 1–8 or by an explicit CLI option), preventing the Windows `EMFILE` failure observed under concurrent first-route compilation without hiding operator overrides.
- [x] Phase 1 — Privy identity: official native/web SDKs, signup/login/account recovery, bilingual EN/VI copy, public App ID/client-ID configuration aliases, and secret-shaped public-value rejection are implemented. Live provider login remains an environment/device acceptance activity.
- [x] Phase 2 — Navigation and shell: whale-first tabs, auxiliary routes, deep links, safe-area behavior, and auth entry points are implemented and regression-covered.
- [x] Phase 3 — Backend health and evidence transparency: validated real API contracts, provenance/freshness/degraded states, bounded recovery, and fail-closed production origins are implemented.
- [x] Phase 4 — Whales: transaction-first activity, qualified holdings, artwork fallbacks, filters, rankings, pagination, and historical/live distinction are implemented without promoting stale evidence.
- [x] Phase 5 — Discover: provider-backed modes, search, capability-aware filters, deterministic ordering/paging, artwork, evidence, and retained-page recovery are implemented.
- [x] Phase 6 — Token detail and research: market/security/holder/transaction/pair evidence, charting, quote review, and recovery are implemented; execution remains locked.
- [x] Phase 7 — Portfolio and wallet intelligence: ownership boundaries, holdings, token rankings, PnL evidence, and watch-only states are implemented.
- [x] Phase 8 — Monitor, alerts, and tracking: bounded feeds, rules, delivery/evaluation history, filtering, sorting, paging, and recovery are implemented.
- [x] Phase 9 — Trenches: provider launch lanes, filters, token handoff, artwork/evidence, and guarded quote entry are implemented.
- [x] Phase 10 — Intelligence and automation: signals, smart money, analytics, CopyTrade configuration, and audit evidence are implemented while activation/signing/submission remain disabled.
- [x] Phase 11 — Accessibility and responsive behavior: semantic controls, minimum touch targets, large-text-safe layouts, localized auth UX, and screen-reader labels are code-verified. Physical assistive-technology evidence remains a QA prerequisite.
- [x] Phase 12 — Recovery, lifecycle, and performance: offline/reconnect, bounded retry, retained evidence, render/page budgets, and lifecycle-safe state are implemented. Physical fault-injection/performance evidence remains a QA prerequisite.
- [x] Phase 13 — Security and privacy: HTTPS/fail-closed origins, redaction, secure storage, public-config validation, secret rejection, and disabled transaction execution are implemented.
- [x] Phase 14 — Release engineering: strict TypeScript, ESLint, 468-test regression gate, Expo compatibility, Doctor 21/21, and configured Android/iOS/Web exports pass. The guarded upstream Noble export warning and physical-device acceptance remain explicitly conditional.

## Foundation and security

- [x] Expo 57 / React Native / Expo Router strict-TypeScript foundation.
- [x] Professional dark, high-contrast visual system uses restrained mint/violet/cyan semantic accents, rounded active navigation, scannable evidence chips and simplified safety panels inspired by supplied references without protected branding or assets.
- [x] Public backend origin is environment-configured; secrets, native credentials, logs, build output, and local env files are ignored.
- [x] Development builds safely fall back to loopback when no public backend origin is configured, while production remains HTTPS-only and fail-closed; wallet signature encoding uses a native-safe byte encoder with no Node standard-library dependency.
- [x] Real responses are runtime-validated; failures never silently become synthetic market data.
- [~] Android secure wallet connection uses official Mobile Wallet Adapter, encrypted authorization/session storage, backend ownership challenge, 24h expiry, cookie/cache revocation, and biometric re-authentication; native wallet support remains unavailable on iOS/web and requires a development build.
- [~] Real Jupiter ExactIn quote review validates exact amounts, route, mint identities, impact, slippage, context slot, and 15-second expiry. Token-identity, provider-readiness, and quote recovery is accessible, busy-safe, and cannot overlap; unavailable readiness remains explicitly execution-blocking. Verified owners can build an unsigned transaction, durably inspect it with an exact serialized-message hash, resolve lookup-table accounts through configured Helius, bind exact Jupiter instruction mints/amounts/slippage, store bounded simulation evidence, and explicitly confirm the quote-bound intent with replay protection. The legacy raw broadcast action is disabled. Wallet signing, managed submission, consumption, and replay-safe execution remain deliberately locked.
- [x] Quote-defining side, amount, unit, and slippage controls plus quote/prepare/confirm actions share one fail-closed busy boundary across quote retrieval, intent preparation, and explicit confirmation; native disabled/editable state, visual state, and assistive busy/disabled semantics stay aligned so displayed inputs cannot diverge from bound evidence.
- [x] Quote expiry is enforced at both preparation and confirmation mutation boundaries, confirmation remains visibly and accessibly disabled after expiry, initial clock state is real rather than zero, and readiness refresh participates in the same atomic evidence-chain lock.
- [x] Explicit app scheme/intent filter and development/preview/production EAS profiles; exact Solana deep-link allowlisting; bounded matching snapshots; HTTPS-only production origins; iOS ATS and Android cleartext/backup denial; diagnostic redaction; and device-local privacy reset are implemented and verified.
- [x] Expo Network drives React Query online state with explicit offline/cached-evidence and recovery disclosure; reads pause and refetch on reconnect, transient retries are bounded, 4xx/abort failures do not retry, and mutations neither retry nor queue for reconnection.
- [x] Untrusted API collections have explicit fail-closed page/render budgets; OHLCV accepts at most 1,000 validated candles and renders at most 240 shape-preserving points with truthful counts; dense token rows avoid unchanged rerenders.

## Approved mobile information architecture

- [x] Whale-first five-tab shell: Whales, Discover, Trenches, Portfolio, More. Whales is the default route and provides Live, Accumulating, Distributing, Wallets, and Alerts views; the mature Monitor alert/delivery workspace remains a hidden routed destination rather than a competing primary tab.
- [x] More catalog preserves AI, Track, CopyTrade, Top Traders, Smart Money, Wallet Tracker, Signals, Heatmap, Snipe List, Analytics, Multicharts, Feed Data, and Claim Monitor destinations.
- [x] Discover is token-feed-first: compact search/Solana scope, mode rail, capability-aware timeframe/filter/provenance controls and dense market rows replace the duplicate hero while preserving Trending/Gainers/Losers/Volume/New Pairs/Hot Searches/Surge/NextBC/Pump Live/Watchlist, server search, refresh, states, and token-detail handoff. Pagination is contract-aware: numeric Trending offsets survive bounded-cache eviction, New Pairs uses its opaque provider cursor, and non-pageable special modes never expose inert continuation. Timeframe/filter controls render only when the selected request contract consumes them and remain hidden during independent server search; saved values are retained for compatible modes.
- [x] Discover rows prioritize validated token imagery with a bottom-right launchpad badge, holder count, 24h volume, market cap, price/change and validated X/Telegram/website presence; repeated DEX text is removed and absent backend imagery uses deterministic initials.
- [x] Discover rows fail closed when provider identity, holder, or age evidence is unavailable: exact mint abbreviations replace blank token names, zero/`new` age placeholders are suppressed, stale/unsafe holder counts are hidden, lower bounds carry `+`, and the selected 1h/6h/24h change is shown beside market cap on the compact second line.
- [x] Discover retry actions are disabled and announced busy during refetch, and no action button is rendered without an executable handler.
- [x] Discover retains already loaded rows when a later cursor page fails, stops automatic retry loops, exposes a polite private error with an explicit busy-safe Retry, and resumes from the failed cursor without resetting filters or ordering.
- [x] Token-logo rendering accepts only bounded directly usable HTTPS artwork, rejects insecure and known hotlink-only media, and recovers from missing, rejected, or failed images with address-deterministic accessible initial avatars while retaining the exact launchpad badge; the fallback is explicitly labeled and never presented as provider artwork.
- [~] Whale Activity is the primary product objective. Mobile defines a named token whale only from a validated current eligible-token holding worth at least $10,000, keeps the observed trade amount independent, and renders “TOKEN Whale bought/sold AMOUNT” plus holding value and snapshot evidence. The authoritative backend now selects recent trades by qualified wallet ownership rather than trade size and returns held-token identity/value/eligibility/time/provenance. Runtime eligible-token allowlist configuration and populated holder/price evidence remain deployment prerequisites; missing configuration has a distinct localized state and missing evidence fails closed as unverified.
- [x] Whale Activity applies useful interaction patterns from the supplied dark radar reference without copying its identity or protected content: accessible buy/sell and minimum-amount controls, latest/largest sorting, dense transaction evidence, and numbered token-flow rankings. Duplicate carousels/dashboards, unsupported caller-impact scores, caller avatars, locked history, and predictive pump claims are intentionally omitted.
- [x] The default Whale Live home is transaction-first: search, view rail, compact evidence status and filters lead directly into dense activity rows; duplicate hero copy, newest-event carousel and aggregate dashboards are intentionally removed from this primary feed.
- [x] Whale rows collapse wholly absent price/market-cap/change fields into one truthful “Market data unavailable” indicator and keep source/quality on the same evidence line, avoiding three meaningless dash chips without fabricating values.
- [x] Live-feed evidence scope is visually reduced to count/status plus an info icon while its complete safety and market-change boundaries remain exposed to assistive technology; the zero-threshold filter uses localized “Any amount” wording.
- [x] Whale Watch applies the Play Store gallery's useful signal-first navigation without copying identity or trade/reward mechanics: accessible token/contract/wallet search, explicit Solana scope, token-specific evidence rows, and truthful live-versus-bounded-historical status from the backend evidence window.
- [x] Whale evidence drill-down is token- and wallet-specific: token detail exposes a bounded newest-first whale chronology with observed flow totals and explicit live/historical scope, while ranked whale rows open the selected public wallet dossier instead of a generic wallet landing page.
- [x] Whale Market Pulse summarizes only the validated bounded evidence window: active token and unique-wallet breadth, known-USD coverage, buy share, net flow, and the largest observed move, with explicit live/historical and incomplete-chain boundaries.
- [x] Whale Watch persists only bounded non-secret view, direction, observed-amount, and sort preferences with strict allowlists, safe fallback, visible save failure, and privacy-reset participation; token/contract/wallet search text is intentionally never persisted.
- [x] Whale Watch activity and wallet-ranking failures expose localized, accessible retry actions; retries are disabled and announced busy while refreshing so repeated taps cannot create overlapping recovery requests.
- [x] Empty Whale wallet rankings show a localized evidence-empty state and never expose wallet-detail navigation with a missing public address.
- [x] Ranked Whale rows open their exact public-wallet dossier, while the aggregate Wallet Intelligence action opens the full research workspace without silently preselecting the first ranking.
- [x] Whale search applies consistently to Live, Accumulating, and Distributing evidence by token symbol, exact mint, or contributing public wallet; unmatched aggregated flows show the truthful existing empty state.
- [x] Whale Wallets search filters qualified rankings by public address, best observed token, or evidence badge while distinguishing a search miss from an empty provider ranking response.
- [x] Whale controls and requests are mode-aware: transaction status/polling appear only for activity modes, Wallets retains ranking search/refresh without unrelated transaction counts, and Alerts omits inactive search/refresh controls.
- [x] Whale Watch preserves dynamic-type scaling, uses a horizontally recoverable primary view rail with 44px minimum targets, wraps KPI evidence on narrow screens, applies narrower safe padding below 380px, and grows relationship rows vertically at 150% text scale while keeping held-token and traded-token artwork anchored left and right.
- [x] Whale amount evidence is retained but context-checked against its accompanying market-cap snapshot; values exceeding that snapshot receive visible bilingual row and bounded Market Pulse disclosures instead of being silently normalized, hidden, or presented as unquestionably comparable.
- [x] Smart-money buy/sell rows display the indexed transaction's observed USD notional when present; profit estimates remain separate and are never substituted for trade size, while missing evidence still fails closed to `—`.
- [x] Whale newest-event and Live rows use a signal-first relationship hierarchy: the validated held-token logo/symbol is on the left, identity/action/amount/market/provenance evidence remains central, and the bought/sold token logo plus direction badge is on the right. Both artwork surfaces use exact-mint deterministic fallback, unsafe held-token media is discarded, the percentage is never mislabeled as an independently measured market-cap change, and wallet aliases/avatars are never fabricated.
- [x] Whale relationship rows expose one localized screen-reader label naming the qualifying holder token, buy/sell action and amount, and traded token; visual artwork order is never the only way the relationship is communicated.
- [x] Whale rows keep qualifying held-token artwork on the left and bought/sold-token artwork on the right; the traded-token artwork carries a bounded backend-derived DEX badge at bottom-right, with a generic exchange glyph when venue evidence is unavailable.
- [x] Whale activity and wallet-ranking recovery never expose raw backend origins, Java/native exception text, or provider request details; localized actionable copy and busy-safe Retry remain available.
- [x] Discover, Trenches, Portfolio analytics, and Monitor live-read recovery use localized actionable copy instead of exposing raw backend origins or native/provider exception details.
- [x] The main Discover feed requests an explicit backend mobile projection retaining token identity, artwork, DEX, price, market cap, liquidity, volume, change, holders, socials, age, provenance, and rendered risk/launch evidence while omitting desktop-only diagnostics; the default backend response remains backward compatible.
- [x] Historical whale and smart-money rows are backend-enriched from the newest exact-token persisted provider pair snapshot when the current live-provider universe has no matching token; bounded price, market cap, one-hour change, volume, holders, artwork and freshness remain nullable and never inferred.
- [x] Non-secret watchlist, validated market snapshots, selected market window, and Discovery filters persist across reloads with idempotent bounded add/remove and explicit reset; saved rows remain visible outside the current feed page, show market provenance/freshness plus exact-token owner alert/delivery status, and disclose device-save failures.
- [~] Token detail fetches normalized backend contracts for chart, holders, holder-cluster evidence, observed transactions, indexed transaction-behavior heuristics, provider-derived early buyers, current risk/security evidence, historical provider security snapshots, smart money, narrative, and pairs with truthful limitations. Shared panel, header, and inline provider recovery is accessible, busy-safe, cannot overlap, and never renders an action without a handler; swap execution remains incomplete.
- [x] Trenches uses real New/Almost Bonded/Migrated launch lanes with bounded keyword, launchpad, market-cap, 24-hour-volume, age, and bonding filters; per-row provenance/freshness; distinct states; detail navigation; refresh; and non-executing quote-review handoff without a chain selector.
- [x] Trenches provider retry is disabled and announced busy during refetch, and no recovery action is rendered without an executable handler.
- [x] Monitor uses real indexed Solana observations; live activity, owner rules, delivery ledger, and evaluation-history failures expose accessible query-specific retries that are busy/disabled during refetch and never render as inert actions. Its provider-backed token table has persistent market windows, provider-derived DEX/search/direction/market filters, column presets, stable two-level sorting, row density, active/reset state, horizontal mobile overflow, cursor pagination with cross-page deduplication before filtering/sorting, freshness/provenance, and an explicit Monitoring-only/not-passed-to-buy boundary. Owner-scoped price/change/volume alert CRUD, paired-cursor persisted evaluation history with explicit 50-row/4-page pagination, and durable delivery diagnostics are complete, with evaluation explicitly separated from delivery/trade authority. Track provides a bounded deduplicated current wallet/KOL window, immutable retained feed history with stable paired replay-sequence cursors and bounded 50-row/4-page rendering, plus lazily loaded provider-backed X/Telegram trends from the existing public social radar capped at 30 tokens and three message samples per token. Its feed, social radar, history, and owner-delivery retries are busy-safe and cannot overlap. Persistent filters, source/freshness/warning evidence, exact-mint handoff, and owner delivery evidence are complete without follow, link, publish, or transaction actions.
- [x] Monitor alert toggle/delete and CopyTrade pause/delete mutations redact private exception text through the bounded public-error classifier, clear stale sibling errors, mutually exclude overlapping destructive state changes, and expose disabled/busy accessibility state on every affected control.
- [x] Monitor alert creation and paused CopyTrade strategy creation are atomic: every editable payload control, mode/toggle, and close action is disabled with matching accessibility state while its request is pending, so the visible review cannot diverge from the submitted payload.
- [x] Portfolio and analytics use provider-backed holdings, explicit watch-only versus verified ownership, allocation/risk evidence, and truthful realized/unavailable PnL provenance.
- [x] Portfolio analytics retry is disabled and announced busy during refetch, and action labels without handlers never become inert buttons.
- [~] CopyTrade provides provenance-aware Top Traders rankings and verified-wallet preview-first durable server configuration for fixed/percentage/proportional sizing, position/daily caps, TP/SL, quote limits, liquidity/market-cap/age filters, direction, delay, concurrency, bounded priority fee, Anti-MEV requirement, minimum holders, trailing stop, and an ordered two-level exit ladder. Health, ranking, saved-strategy, position, and execution-audit failures have accessible busy-safe recovery. Safety controls remain privacy-resettable on-device and are validated and persisted with the paused server strategy, but are not claimed as execution-enforced. Mobile always creates paused strategies; activation, confirmation, signing, closing, and submission remain omitted pending the full transaction safety gate.
- [x] AI Intelligence provides fail-closed advisory recommendations, public read-only paper performance/positions/research, bounded operational/mutation/lease/cycle integrity evidence, verified-wallet 31-phase governance evidence, and owner-scoped durable GMGN provider discovery history with exact Solana identity, provenance, freshness, quality, confidence, and mint-verification evidence. Advisory, paper, history, and governance provider failures have accessible busy-safe query-specific recovery. Every contract remains simulation-only/read-only with kill-switch, fail-closed no-replay recovery, and execution-disabled boundaries enforced at runtime.
- [x] Signals provides filterable 24h/7d signature-backed events with cursor pagination, ingestion/freshness evidence, exact-mint detail handoff, accessible busy-safe provider recovery, and no export or transaction authority; valid degraded backend evidence remains visible.
- [x] Heatmap provides trustworthy provider market rows ordered by volume, gain/loss intensity, liquidity, trust warnings, excluded-row counts, freshness, exact-mint detail handoff, and accessible busy-safe provider recovery.
- [x] Claim Monitor provides read-only Solana RPC health, confirmed/detected/unpaid claim evidence, first-observed status, signature provenance, accessible busy-safe provider recovery, and explicit no-claim/no-wallet-action boundaries.
- [x] Smart Money uses non-deprecated provider/indexed Top Traders evidence, filters only Smart Money/Whale classifications, presents historical PnL/win-rate/drawdown/reliability limitations, joins public holdings plus indexed realized PnL without copy or follow actions, and exposes accessible busy-safe ranking recovery.
- [x] Wallet Tracker persists at most 50 sanitized, deduplicated public addresses and labels on-device, validates exact Solana keys, shows real backend holdings/prices and indexed PnL limitations with accessible busy-safe holdings and partial-PnL recovery, and participates in privacy reset without implying ownership.
- [x] Snipe List persists at most 20 exact token mints, sanitized notes, and bounded visual price thresholds on-device; token identity/price refresh is backend-backed with per-candidate accessible busy-safe failure recovery, while the UI explicitly denies background-alert and execution claims and directs durable rules to Monitor.
- [x] Multicharts persists up to four unique exact token mints and one shared timeframe, renders only real validated token/OHLCV responses with independent refresh/error/provenance states, provides accessible busy-safe chart-query recovery without inert actions, and inserts no default or synthetic markets.
- [x] Analytics summarizes only exact-mint real market records across breadth, liquidity, volume, momentum, fresh pairs, and historical trader evidence, with explicit exclusions, provenance, staleness, missing-field, and non-predictive boundaries.
- [x] Feed Data observes validated provider inventory, configuration/connection/subscription state, useful-traffic status, last success/error, cooldown/rate-limit pressure, received/decoded/persisted/dropped/ignored totals and refresh deltas, event-bus persistence pressure, consumer freshness, evidence availability, remediation guidance, and durable ingestion jobs through GET-only backend contracts. Market, gainers, fresh-pair, trader, connection, and diagnostic failures have exact-query busy-safe recovery; nominal configuration cannot imply healthy delivery, while active probes, provider mutation, replay, credentials, and raw record inspection are absent.
- [~] Settings provides persistent interpolation-safe English/Vietnamese strings across primary tabs, token detail, quote review, every auxiliary intelligence/research/operations route, and full Settings safety copy, plus reduced-motion preference, telemetry consent default-off, and privacy reset. Device screen-reader/dynamic-type verification remains incomplete.

## Verification

- [x] Unit tests for response validation and financial formatting.
- [x] Compact USD formatting is deterministic on Hermes and JavaScript engines without `Intl` compact-notation support, including bounded K/M/B/T/Q units and correct negative-currency signs.
- [x] Component and client interaction coverage includes accessible token navigation/watchlist controls, verified unsigned-swap build/inspect/simulate/explicit-confirm routing with no signing/submission call, durable validated watchlist storage/window fallback and exact-token alert-delivery evidence, Monitor table persistence/filter/two-level-sort/preset/overflow/decision-boundary behavior, Trenches quote handoff plus combined filter/input/missing-evidence behavior, alert financial-input gating, localized CopyTrade eligibility plus durable-field validation/paused payload, device-local safety-preview validation/persistence/server non-submission, and AI advisory qualification, hostile input/privacy boundaries, localized exact-mint Signal handoff, distinct localized tracked-wallet and Snipe destructive intents, truthful localized Feed Data labels plus recovery counters/subscription/cooldown/failure evidence, localization persistence/interpolation/fallback, direct busy/error/empty/retry state coverage across primary markets, Monitor, Portfolio, Token Detail, quote review, Settings reset, AI, CopyTrade, market intelligence, wallet intelligence, Operations, and Research, guarded inline asynchronous controls, and a static all-route Pressable/TextInput semantics gate.
- [x] Public error presentation is fail-closed: audited routes never render exception messages verbatim; read failures use localized domain copy, user-action failures cross a centralized sanitizer, and regression tests reject origin/native/secret-bearing payload leakage.
- [x] Wallet-adapter exceptions are sanitized before entering shared session state and every identity gate renders localized public copy; Monitor table failures and historical CopyTrade execution errors also remain private without hiding their status or recovery controls.
- [x] Provider-supplied diagnostic reasons never render verbatim in Track, Monitor, swap simulation or Operations; a tested fail-closed classifier maps them to seven localized public categories while unknown details remain private.
- [x] Monitor table preference persistence clears stale storage failures synchronously and applies only the newest save failure; pagination regression interactions and cleanup run inside React `act`, keeping the complete test gate free of asynchronous state-update warnings.
- [x] Monitor cursor-page failures preserve already validated rows, expose localized private pagination-only recovery, prevent overlapping retries, and resume the failed cursor without replacing the table or leaking transport details.
- [x] Repository-wide ESLint is warning-free for source-owned code; Expo-generated router declarations are excluded from lint noise while remaining covered by strict TypeScript compilation.
- [x] The package lint gate invokes the declared local ESLint executable directly over `app` and `src`, avoiding implicit global `npx` or Expo CLI delegation while retaining the Expo flat configuration.
- [x] A command-contract regression verifies that typecheck, lint, test, and CI test scripts start from declared local tool dependencies and that lint remains scoped to source-owned roots.
- [x] MOBILE accepts the WEB indexer-health schema-v1 envelope through a strict read-only client contract, distinguishes unavailable from unhealthy evidence, rejects contradictory status/health pairs, and preserves `automationSafe: false` without enabling execution.
- [x] Feed Data renders the indexer envelope as a localized read-only operations card with feed-tab-only polling, coordinated refresh/loading, independent busy-safe recovery, distinct configured/unavailable/healthy/degraded states, bounded tip/freshness/lag/ingestion/quality evidence, no inferred zeroes, and accessible non-execution semantics.
- [~] Android, iOS, and web production bundles pass; Expo native-config introspection proves ATS/cleartext/backup policy, automated accessibility tests cover every primary-tab/Settings Pressable and TextInput, simulated offline/reconnect/query-policy tests pass, and adversarial collection/render/security checks pass. Real-device screen-reader/dynamic-type/connectivity/performance verification remains incomplete.
- [x] Android emulator verification at 1.3× system font scale preserves single-line whale amounts and KPI values with bounded font fitting; the original device font scale is restored after testing.
- [x] Android development-build installation and runtime startup are verified on the API 37 emulator with Java 17 and the configured Android SDK; the required native splash-screen module is explicitly installed, Whales and Discover render, primary-tab navigation works, and backend-offline/provider-empty recovery states remain truthful without a fatal native or React Native error.
- [x] Final full requirement and regression audit is recorded in `FINAL_AUDIT.md`; implementation-complete rows are separated from backend-contract and physical-device blockers without overstating completion.
- [x] Android debug startup disables only shake-to-open sensing through an idempotent, fail-closed Expo config plugin; keyboard/ADB developer access remains available, and API 37 cold launch completes without ANR.
- [x] Unconfigured Android development builds use the emulator's host alias for the local backend, while iOS/web keep loopback; explicit origins and production HTTPS enforcement remain authoritative.
- [x] Verified development sessions refuse tracked dirty state, embed the exact Git HEAD in Expo config, and emit a bounded device-log build marker so QA can bind runtime evidence to an immutable MOBILE commit.
- [x] Privacy/accessibility regression assertions verify Monitor and CopyTrade mutation sanitization semantically across formatting changes; the full Jest release gate is restored.
- [x] Monitor and CopyTrade mutual-exclusion tests enforce reset-before-mutate ordering with formatting-independent semantic matchers for all four mutation paths.
- [x] Release evidence reports test totals from the immutable committed checkout and excludes concurrent uncommitted test inventory.
- [x] SnipeCard interaction tests await initial token-evidence settlement before teardown, preventing post-test React updates and overlapping `act` warnings.
- [x] The known Noble/Metro strict-exports fallback is version-pinned, requires both installed fallback files, rejects silent root cryptography overrides, and fails visibly when its audited compatibility assumptions drift.
- [x] A repository-local `diagnostics:expo` command checks SDK package compatibility without global tools or implicit `npx`; its contract is regression-tested and fails closed whenever package versions drift.
- [x] Expo, Constants, Dev Client, and Router match SDK 57's expected patch releases; local compatibility diagnostics, static gates, regressions, public config, and Android bundle export pass after the lockfile update.
- [x] Every SnipeCard query test awaits a rendered settled state (success or recovered error removal), keeping focused and full CI runs free of order-dependent React update warnings.
- [x] The current npm advisory chain is explicitly bounded to Expo configuration tooling: `uuid` and `xcode` are not direct dependencies or runtime imports, exact transitive versions are guarded, and any boundary drift fails the security regression gate pending upstream remediation.
- [x] Expo Doctor is an exact repository-local development dependency with a tested no-`npx` command; all 21 checks pass and dynamic Expo config consumes the static config input while preserving bounded immutable-build provenance.
- [x] MOBILE-A11Y-201..220: Discover, Monitor, and Trenches expose correct tab/radio/checkbox/switch relationships; pagination busy state is truthful; filter visibility is announced; search and numeric inputs are natively bounded; malformed decimal filters are normalized; bonding progress cannot exceed 100.
- [x] MOBILE-DATA-221..240: Portfolio, Wallet Intelligence, Research, Whales, Monitor, and CopyTrade enforce bounded exact-address/search/financial input; research/alert decimals normalize before state; radio choices expose groups; wallet and tracked-wallet mutations are mutually exclusive.
- [x] MOBILE-TOUCH-241..260: Discover, Monitor, and Trenches mode/period/filter/retry/DEX/reset/apply/refresh/window/preset/direction/sort/density/pagination/lane/launchpad controls meet a 44px mobile touch-target floor with regression coverage.
- [x] MOBILE-DATA-261..268 / MOBILE-QA-021: Trenches launchpad options normalize and deduplicate provider DEX evidence before limiting/rendering, reserve the local All sentinel, filter null-safely, recover stale selection, and never print undefined provenance.
- [x] MOBILE-DATA-281..288: Monitor DEX options normalize and deduplicate provider evidence before limiting/rendering, reserve the local All sentinel, filter missing values safely, recover stale selections, and never print blank/undefined provenance.
- [x] MOBILE-DATA-301..308: Token Detail, Quote Review, Operations, Discover, Monitor, Trenches, Market Intelligence, and Wallet Intelligence trim provider provenance and localize blank/missing DEX or source evidence.
- [x] MOBILE-DATA-321..340: Twenty distinct evidence labels across Operations, Whales, Wallet Intelligence, Track, Token Detail, Discover, Monitor, and Trenches reject whitespace-only quality/source/provider/method/DEX/token text and use localized fallbacks.
- [x] MOBILE-DATA-341..360: Twenty live-feed labels across CopyTrade, Market Intelligence, Monitor, Trenches, Whales, and Track normalize blank quality/source/provider/RPC/channel/status text; provider lists trim, deduplicate, and fall back truthfully.
- [x] MOBILE-QA-281 / MOBILE-TOUCH-361..379: Monitor regression cleanup is order-safe and 19 auxiliary-route back, selector, toggle, pause, remove, input, filter, and retry control families meet the 44px mobile touch-target floor.
- [x] MOBILE-QA-281 / MOBILE-TOUCH-380..398: the grouped Monitor/touch regression has an explicit loaded-run budget and 19 additional tab, period, chip, timeframe, alert-management, trade-side, and slippage control families meet the 44px touch floor.
- [x] MOBILE-TOUCH-401..414 / MOBILE-DATA-415: fifteen remaining compact action/search/filter/recovery families are reachable at 44px-equivalent geometry, and quote amount input is natively and semantically bounded before request state.
- [x] MOBILE-QA-282 / MOBILE-DATA-416: the loaded AsyncSurface/touch regression pair is deterministic across three consecutive runs, and whale identity requires an eligible famous-token holding strictly above $10,000.
- [x] MOBILE-I18N-417..424: Discover token rows localize token-detail/watchlist actions, truthful age/holder evidence, holder counts, volume shorthand, and social accessibility summaries in English and Vietnamese.
- [x] MOBILE-I18N-425..429: Discover and Token Detail localize provider token-artwork success/fallback semantics plus known and unknown DEX badge accessibility labels.
- [x] MOBILE-I18N-430 / MOBILE-A11Y-431..433: Discover localizes market-cap shorthand and binds price, market cap, selected change period, and missing-change truthfulness into one accessible metric snapshot.
- [x] MOBILE-I18N-434 / MOBILE-DATA-435..438: Whale flow chronology formats seconds/milliseconds in EN/VI and fails closed for invalid time; market chips localize market cap and partial missing evidence.
- [x] MOBILE-I18N-439..442 / MOBILE-DATA-443..444: live Whale cards localize seconds/minutes/hours/days and reject malformed or future observation ages instead of showing misleading values.
- [x] MOBILE-DATA-445..459: Discover, Trenches, Operations, and Monitor share localized defensive relative-age handling for provider seconds/milliseconds, malformed/future evidence, and day rollover where supported.
- [x] MOBILE-I18N-460..464 / MOBILE-DATA-465..466: AI and Track chronology follows EN/VI and defensively accepts provider seconds/milliseconds/ISO strings while rejecting malformed values.
- [x] MOBILE-I18N-467..469: Token Detail early-buyer, security-history, and Whale chronology uses localized fail-closed provider-time formatting.
- [x] MOBILE-I18N-470..482: Portfolio, Wallet Intelligence, Market Intelligence, Token Detail, and Operations quantities use selected EN/VI separators and fail closed for invalid numbers.
- [x] MOBILE-I18N-483..498: Token Detail and Wallet Intelligence percentages/fixed decimals use bounded EN/VI formatting and fail closed for invalid numbers.
- [x] MOBILE-FILTER-515: Monitor persists observed DEX selections in canonical lowercase form, constrains mixed-provider rows, and restores all rows on reset with rendered regression coverage.
- [x] MOBILE-A11Y-517: Discover and Trenches detail/watchlist-or-quote actions render as sibling controls; live web DOM verification reports zero nested buttons and no fresh hydration errors.
- [x] MOBILE-QA-276: bundled Node is explicitly available to Expo child processes; Expo SDK dependencies are patch-aligned and repository-local Expo Doctor passes all 21 checks.
- [x] MOBILE-AUTH-518: Privy identity is available from More on web and native through isolated official SDK adapters; email OTP and Google are supported natively, while web uses the configured Privy modal.
- [x] MOBILE-AUTH-519: missing Privy App/Client IDs fail closed with an explicit setup state; no credential, access token, or provider exception is persisted or rendered.
- [x] MOBILE-AUTH-520: Privy authentication remains separate from verified Solana ownership and does not grant signing, trading, CopyTrade activation, or transaction submission authority.
- [x] MOBILE-SEC-521: patched axios/ws transitive overrides remove high/critical npm production advisories; remaining findings are moderate.
- [x] MOBILE-BUNDLE-522: platform-specific Privy modules bundle successfully for Expo web and Android; native jose resolves to its WebCrypto implementation instead of Node zlib.
- [x] MOBILE-DEV-533: a clean static Expo web export can be served through the loopback-only extensionless-route harness when interactive Metro is blocked by Windows `EMFILE`; Whales, Discover, Trenches, More, and Auth were exercised from the exact export.
- [x] MOBILE-AUTH-534: malformed public Privy App IDs are rejected before either official SDK mounts, so an invalid local value produces the explicit setup-required screen instead of a blank application.
- [x] MOBILE-WEB-535: responsive Whales and price-chart dimensions use a deterministic server/first-client snapshot and switch to live viewport dimensions after hydration.
- [x] MOBILE-TOOLCHAIN-536: Expo SDK 57 runtime, constants, and font packages are aligned to the Doctor-required patch line and protected by a focused version assertion.
- [x] MOBILE-EXPORT-537: a fresh isolated web export contains all 26 route HTML artifacts and serves `/`, `/whales`, `/discover`, and `/auth` with HTTP 200 through the loopback export harness.
- [x] MOBILE-WEB-538: static web hydration uses deterministic safe-area metrics and defers the browser-dependent tab navigator until after the matching server/first-client snapshot; Whales, Discover, and Auth hydrate without React #418.
- [x] MOBILE-QA-539: the loopback export harness has an explicit opt-in, bounded browser-console capture/reset endpoint that detects hydration errors without changing normal server or production behavior.
