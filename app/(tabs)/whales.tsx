import { Ionicons } from "@expo/vector-icons";
import { useQuery } from "@tanstack/react-query";
import { useRouter } from "expo-router";
import { useMemo, useState } from "react";
import { Pressable, RefreshControl, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { fetchTopTraders, fetchTrackFeed } from "@/api/client";
import type { TopTrader, TrackNotification } from "@/api/schema";
import { compactUsd } from "@/lib/format";
import { aggregateWhaleActivity, buildWhaleMarketPulse, filterWhaleEvents, isWhaleActivity, type WhaleEventDirection, type WhaleEventSort, type WhaleFlow, type WhaleMarketPulse } from "@/lib/whale-activity";
import { useSettings } from "@/settings/SettingsProvider";
import { colors, spacing } from "@/theme";

type Mode = "live" | "accumulating" | "distributing" | "wallets" | "alerts";
const modes: Mode[] = ["live", "accumulating", "distributing", "wallets", "alerts"];

export default function WhalesScreen() {
  const { t } = useSettings();
  const router = useRouter();
  const [mode, setMode] = useState<Mode>("live");
  const [direction, setDirection] = useState<WhaleEventDirection>("all");
  const [minimumUsd, setMinimumUsd] = useState(0);
  const [sort, setSort] = useState<WhaleEventSort>("latest");
  const [query, setQuery] = useState("");
  const feed = useQuery({ queryKey: ["whale-activity"], queryFn: ({ signal }) => fetchTrackFeed(signal), refetchInterval: 30_000 });
  const rankings = useQuery({ queryKey: ["whale-wallet-rankings"], queryFn: ({ signal }) => fetchTopTraders("30D", signal), enabled: mode === "wallets" });
  const allEvents = useMemo(() => (feed.data?.notifications ?? []).filter(isWhaleActivity).sort((a, b) => b.observedAt - a.observedAt), [feed.data]);
  const events = useMemo(() => filterWhaleEvents(allEvents, { direction, minimumUsd, sort, query }), [allEvents, direction, minimumUsd, query, sort]);
  const flows = useMemo(() => aggregateWhaleActivity(allEvents), [allEvents]);
  const pulse = useMemo(() => buildWhaleMarketPulse(allEvents), [allEvents]);
  const totals = useMemo(() => allEvents.reduce((value, item) => {
    const sell = item.type === "whale_sell" || item.type === "smart_take_profit";
    return { buys: value.buys + (sell ? 0 : item.amountUsd ?? 0), sells: value.sells + (sell ? item.amountUsd ?? 0 : 0) };
  }, { buys: 0, sells: 0 }), [allEvents]);
  const wallets = useMemo(() => (rankings.data?.traders ?? []).filter((item) => item.badge === "Whale" || item.badge === "Smart Money"), [rankings.data]);
  const historical = feed.data?.evidenceWindow?.smartMoneyHistorical || feed.data?.evidenceWindow?.whaleTransactionsHistorical;
  const evidenceCount = historical ? allEvents.length : (feed.data?.coverage?.whaleTransactions.currentRecordCount ?? feed.data?.coverage?.whaleTransactions.recordCount ?? 0);
  const refresh = () => mode === "wallets" ? rankings.refetch() : feed.refetch();
  return <SafeAreaView style={styles.safe} edges={["top"]}>
    <ScrollView refreshControl={<RefreshControl refreshing={feed.isRefetching || rankings.isRefetching} onRefresh={refresh} tintColor={colors.accent} />} contentContainerStyle={styles.content}>
      <View style={styles.header}>
        <View style={styles.heroIcon}><Ionicons name="water" size={22} color={colors.accent} /></View>
        <View style={styles.flex}><Text style={styles.eyebrow}>TERMINAL DEX · ONCHAIN INTELLIGENCE</Text><Text accessibilityRole="header" style={styles.title}>{t("whales")}</Text><Text style={styles.subtitle}>{t("whaleObjective")}</Text></View>
        <View accessibilityRole="summary" style={styles.status}><View style={[styles.dot, (historical || feed.data?.coverage?.whaleTransactions.dataQuality === "no_recent_records") && styles.stale]} /><Text style={styles.statusText}>{evidenceCount} {t(historical ? "historicalEvidence" : "whaleMode_live").toUpperCase()}</Text></View>
      </View>
      <View style={styles.searchRow}><View style={styles.searchBox}><Ionicons name="search" size={16} color={colors.muted} /><TextInput accessibilityLabel={t("searchWhaleActivity")} placeholder={t("searchWhaleActivity")} placeholderTextColor={colors.muted} value={query} onChangeText={setQuery} autoCapitalize="none" autoCorrect={false} style={styles.searchInput} />{query ? <Pressable accessibilityRole="button" accessibilityLabel={t("resetWhaleFilters")} onPress={() => setQuery("")}><Ionicons name="close-circle" size={17} color={colors.muted} /></Pressable> : null}</View><View accessible accessibilityLabel={t("whaleChainScope")} style={styles.chain}><Text style={styles.chainMark}>≋</Text><Text style={styles.chainText}>SOL</Text></View></View>
      {allEvents.length ? <>
        <View style={styles.justInHead}><View><Text style={styles.justInTitle}>{t("whaleJustIn")}</Text><Text style={styles.justInHint}>{t("whaleJustInHint")}</Text></View><View style={styles.pulse}><View style={[styles.dot, historical && styles.stale]} /><Text style={styles.pulseText}>{allEvents.length} {t(historical ? "historicalEvidence" : "whaleMode_live").toUpperCase()}</Text></View></View>
        <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.justInRow}>{allEvents.slice(0, 5).map((item) => <JustInCard key={item.id} item={item} onOpen={() => router.push({ pathname: "/token/[address]", params: { address: item.tokenAddress } })} />)}</ScrollView>
        <View style={styles.kpis}><Kpi label={t("whaleTrackedWindow")} value={String(allEvents.length)} /><Kpi label={t("whaleBuyVolume")} value={compactUsd(totals.buys)} positive /><Kpi label={t("whaleNetFlow")} value={`${totals.buys - totals.sells >= 0 ? "+" : ""}${compactUsd(totals.buys - totals.sells)}`} positive={totals.buys >= totals.sells} negative={totals.buys < totals.sells} /></View>
        <MarketPulse pulse={pulse} historical={Boolean(historical)} />
      </> : null}
      <View accessibilityRole="tablist" style={styles.tabs}>{modes.map((item) => <Pressable key={item} accessibilityRole="tab" accessibilityState={{ selected: mode === item }} accessibilityLabel={t("selectWhaleView", { view: t(`whaleMode_${item}`) })} onPress={() => setMode(item)} style={[styles.tab, mode === item && styles.tabActive]}><Text style={[styles.tabText, mode === item && styles.tabTextActive]}>{t(`whaleMode_${item}`)}</Text></Pressable>)}</View>
      <Text accessibilityRole="summary" style={styles.boundary}>{t("whaleEvidenceBoundary")}</Text>
      {mode === "live" ? <WhaleControls direction={direction} minimumUsd={minimumUsd} sort={sort} onDirection={setDirection} onMinimum={setMinimumUsd} onSort={setSort} onReset={() => { setDirection("all"); setMinimumUsd(0); setSort("latest"); setQuery(""); }} /> : null}
      {mode === "wallets" ? <WalletRankings rows={wallets} loading={rankings.isLoading} error={rankings.error?.message} onOpen={(walletAddress) => router.push({ pathname: "/wallet-intelligence", params: { address: walletAddress } })} /> : mode === "alerts" ? <AlertsHandoff onOpen={() => router.push("/(tabs)/monitor")} /> : feed.isLoading ? <State text={t("loadingWhaleActivity")} /> : feed.isError ? <State error text={feed.error.message} /> : !allEvents.length ? <WhaleFeedUnavailable onWallets={() => setMode("wallets")} /> : mode === "live" ? <LiveEvents rows={events} onReset={() => { setDirection("all"); setMinimumUsd(0); setSort("latest"); setQuery(""); }} onOpen={(address) => router.push({ pathname: "/token/[address]", params: { address } })} /> : <FlowList rows={flows.filter((item) => mode === "accumulating" ? item.netUsd > 0 : item.netUsd < 0)} onOpen={(address) => router.push({ pathname: "/token/[address]", params: { address } })} />}
    </ScrollView>
  </SafeAreaView>;
}

function JustInCard({ item, onOpen }: { item: TrackNotification; onOpen: () => void }) {
  const { t } = useSettings();
  const buy = item.type === "whale_buy" || item.type === "smart_buy";
  return <Pressable accessibilityRole="button" accessibilityLabel={t("openWhaleEvent", { symbol: item.tokenSymbol })} onPress={onOpen} style={styles.justInCard}><View style={styles.row}><Text style={styles.justInSymbol}>{item.tokenSymbol}</Text><Text style={[styles.justInType, { color: buy ? colors.positive : colors.negative }]}>{t(buy ? "whaleBuy" : "whaleSell")}</Text></View><Text style={styles.justInAmount}>{item.amountUsd == null ? "—" : compactUsd(item.amountUsd)}</Text><Text numberOfLines={1} style={styles.meta}>{item.wallet ? short(item.wallet) : item.source}</Text><Text style={styles.justInTime}>{new Date(item.observedAt).toLocaleTimeString()}</Text></Pressable>;
}

function WhaleControls({ direction, minimumUsd, sort, onDirection, onMinimum, onSort, onReset }: { direction: WhaleEventDirection; minimumUsd: number; sort: WhaleEventSort; onDirection: (value: WhaleEventDirection) => void; onMinimum: (value: number) => void; onSort: (value: WhaleEventSort) => void; onReset: () => void }) {
  const { t } = useSettings();
  const filtered = direction !== "all" || minimumUsd !== 0 || sort !== "latest";
  return <View accessibilityRole="summary" style={styles.controls}><View style={styles.row}><Text style={styles.controlsTitle}>{t("whaleControls")}</Text>{filtered ? <Pressable accessibilityRole="button" accessibilityLabel={t("resetWhaleFilters")} onPress={onReset}><Text style={styles.resetText}>{t("resetWhaleFilters")}</Text></Pressable> : null}</View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.controlRow}>{(["all", "buy", "sell"] as const).map((item) => <Control key={item} label={t(`whaleDirection_${item}`)} accessibilityLabel={t("selectWhaleDirection", { direction: t(`whaleDirection_${item}`) })} active={direction === item} onPress={() => onDirection(item)} />)}<View style={styles.controlDivider} />{[0, 25_000, 100_000].map((amount) => <Control key={amount} label={amount ? `≥ ${compactUsd(amount)}` : `${t("whaleMinimum")} $0`} accessibilityLabel={t("selectWhaleMinimum", { amount: compactUsd(amount) })} active={minimumUsd === amount} onPress={() => onMinimum(amount)} />)}<View style={styles.controlDivider} />{(["latest", "largest"] as const).map((item) => <Control key={item} label={t(`whaleSort_${item}`)} accessibilityLabel={t("selectWhaleSort", { sort: t(`whaleSort_${item}`) })} active={sort === item} onPress={() => onSort(item)} />)}</ScrollView></View>;
}

function Control({ label, accessibilityLabel, active, onPress }: { label: string; accessibilityLabel: string; active: boolean; onPress: () => void }) { return <Pressable accessibilityRole="radio" accessibilityLabel={accessibilityLabel} accessibilityState={{ checked: active }} onPress={onPress} style={[styles.control, active && styles.controlActive]}><Text style={[styles.controlText, active && styles.controlTextActive]}>{label}</Text></Pressable>; }

function Kpi({ label, value, positive, negative }: { label: string; value: string; positive?: boolean; negative?: boolean }) { return <View style={styles.kpi}><Text style={styles.kpiLabel}>{label}</Text><Text style={[styles.kpiValue, positive && styles.good, negative && styles.bad]}>{value}</Text></View>; }

function MarketPulse({ pulse, historical }: { pulse: WhaleMarketPulse; historical: boolean }) {
  const { t } = useSettings();
  const largest = pulse.largestEvent;
  return <View accessible accessibilityRole="summary" style={styles.pulsePanel}><View style={styles.row}><View><Text style={styles.pulsePanelTitle}>{t("whaleMarketPulse")}</Text><Text style={styles.pulsePanelHint}>{t("whaleMarketPulseScope", { scope: t(historical ? "historicalEvidence" : "whaleMode_live") })}</Text></View><Text style={[styles.pulseBias, { color: pulse.netUsd >= 0 ? colors.positive : colors.negative }]}>{pulse.buyShare == null ? "—" : `${Math.round(pulse.buyShare * 100)}% ${t("whaleBuyShare")}`}</Text></View><View style={styles.pulseMetrics}><Metric label={t("activeTokens")} value={String(pulse.activeTokens)} /><Metric label={t("uniqueWallets")} value={String(pulse.uniqueWallets)} /><Metric label={t("whaleUsdCoverage")} value={`${pulse.knownAmountCount}/${pulse.eventCount}`} /></View>{largest ? <Text style={styles.pulseLargest}>{t("largestObservedMove", { symbol: largest.tokenSymbol, amount: compactUsd(largest.amountUsd) })}</Text> : null}</View>;
}

function LiveEvents({ rows, onReset, onOpen }: { rows: TrackNotification[]; onReset: () => void; onOpen: (address: string) => void }) {
  const { t } = useSettings();
  if (!rows.length) return <View style={styles.state}><Ionicons name="filter" size={24} color={colors.muted} /><Text style={styles.stateText}>{t("noWhaleFilterMatches")}</Text><Pressable accessibilityRole="button" accessibilityLabel={t("resetWhaleFilters")} onPress={onReset} style={styles.secondary}><Text style={styles.secondaryText}>{t("resetWhaleFilters")}</Text></Pressable></View>;
  return <View>{rows.map((item) => { const buy = item.type.endsWith("buy"); return <Pressable key={item.id} accessibilityRole="button" accessibilityLabel={t("openWhaleEvent", { symbol: item.tokenSymbol })} onPress={() => onOpen(item.tokenAddress)} style={styles.card}>
    <View style={[styles.direction, { backgroundColor: buy ? colors.accentDim : "#401b24" }]}><Ionicons name={buy ? "arrow-down" : "arrow-up"} size={17} color={buy ? colors.positive : colors.negative} /></View>
    <View style={styles.flex}><View style={styles.row}><View style={styles.flex}><Text style={styles.cardTitle}>{item.tokenSymbol}</Text><Text style={[styles.actionText, { color: buy ? colors.positive : colors.negative }]}>{t(buy ? "whaleBuy" : "whaleSell")} {item.amountUsd == null ? "—" : compactUsd(item.amountUsd)}</Text></View><View style={styles.eventContext}><Text numberOfLines={1} style={styles.walletText}>{item.wallet ? short(item.wallet) : t("classifiedWallet")}</Text><Text style={styles.meta}>{item.market.marketCap != null ? `MC ${compactUsd(item.market.marketCap)} · ` : ""}{new Date(item.observedAt).toLocaleTimeString()}</Text></View></View><Text style={styles.evidence}>{item.source} · {item.dataQuality}</Text></View>
  </Pressable>; })}</View>;
}

function WhaleFeedUnavailable({ onWallets }: { onWallets: () => void }) {
  const { t } = useSettings();
  return <View accessible accessibilityRole="summary" style={styles.notice}><Ionicons name="cloud-offline-outline" size={24} color={colors.warning} /><Text style={styles.noticeTitle}>{t("whaleFeedUnavailableTitle")}</Text><Text style={styles.noticeText}>{t("whaleFeedUnavailableBody")}</Text><Pressable accessibilityRole="button" accessibilityLabel={t("viewRankedWhaleWallets")} onPress={onWallets} style={styles.primary}><Text style={styles.primaryText}>{t("viewRankedWhaleWallets")}</Text></Pressable></View>;
}

function FlowList({ rows, onOpen }: { rows: WhaleFlow[]; onOpen: (address: string) => void }) {
  const { t } = useSettings();
  if (!rows.length) return <State text={t("noWhaleFlow")} />;
  return <View>{rows.map((item, index) => <Pressable key={item.tokenAddress} accessibilityRole="button" accessibilityLabel={t("openWhaleFlow", { symbol: item.tokenSymbol })} onPress={() => onOpen(item.tokenAddress)} style={styles.flowCard}><View style={styles.row}><View style={styles.rankMini}><Text style={styles.rankMiniText}>{index + 1}</Text></View><View style={styles.flex}><Text style={styles.cardTitle}>{item.tokenSymbol}</Text><Text style={styles.meta}>{t("whaleRank", { rank: index + 1 })}</Text></View><Text style={[styles.amount, { color: item.netUsd >= 0 ? colors.positive : colors.negative }]}>{item.netUsd >= 0 ? "+" : ""}{compactUsd(item.netUsd)}</Text></View><View style={styles.metrics}><Metric label={t("whaleBuys")} value={`${item.buys} · ${compactUsd(item.buyUsd)}`} /><Metric label={t("whaleSells")} value={`${item.sells} · ${compactUsd(item.sellUsd)}`} /><Metric label={t("uniqueWallets")} value={String(item.uniqueWallets)} /></View><Text style={styles.evidence}>{t("lastObserved")}: {new Date(item.latestObservedAt).toLocaleString()}</Text></Pressable>)}</View>;
}

function WalletRankings({ rows, loading, error, onOpen }: { rows: TopTrader[]; loading: boolean; error?: string; onOpen: (address: string) => void }) {
  const { t } = useSettings();
  if (loading) return <State text={t("loadingWalletRankings")} />;
  if (error) return <State error text={error} />;
  return <View>{rows.slice(0, 20).map((item) => <Pressable key={item.address} accessibilityRole="button" accessibilityLabel={t("inspectWallet", { badge: item.badge, address: item.address })} onPress={() => onOpen(item.address)} style={styles.card}><View style={styles.rank}><Text style={styles.rankText}>{item.rank}</Text></View><View style={styles.flex}><Text style={styles.cardTitle}>{item.bestToken || t("unknownToken")} · {item.badge}</Text><Text style={styles.walletText}>{short(item.address)}</Text><Text style={styles.meta}>{item.trades} {t("observedTrades")} · {item.winRate.toFixed(1)}% {t("winRate")}</Text></View><View style={styles.eventContext}><Text style={[styles.amount, { color: item.pnlUsd >= 0 ? colors.positive : colors.negative }]}>{compactUsd(item.pnlUsd)}</Text><Text style={styles.meta}>{t("bestObservedToken")}</Text></View></Pressable>)}<Pressable accessibilityRole="button" accessibilityLabel={t("openWalletIntelligence")} onPress={() => onOpen(rows[0]?.address ?? "")} style={styles.primary}><Text style={styles.primaryText}>{t("openWalletIntelligence")}</Text></Pressable></View>;
}

function AlertsHandoff({ onOpen }: { onOpen: () => void }) { const { t } = useSettings(); return <View style={styles.notice}><Ionicons name="notifications-outline" size={24} color={colors.warning} /><Text style={styles.noticeTitle}>{t("whaleAlertsNotSupported")}</Text><Text style={styles.noticeText}>{t("whaleAlertsBoundary")}</Text><Pressable accessibilityRole="button" accessibilityLabel={t("openAlertManagement")} onPress={onOpen} style={styles.primary}><Text style={styles.primaryText}>{t("openAlertManagement")}</Text></Pressable></View>; }
function Metric({ label, value }: { label: string; value: string }) { return <View style={styles.metric}><Text style={styles.metricLabel}>{label}</Text><Text style={styles.metricValue}>{value}</Text></View>; }
function State({ text, error }: { text: string; error?: boolean }) { return <View accessible accessibilityRole={error ? "alert" : "summary"} style={styles.state}><Ionicons name={error ? "warning" : "pulse"} size={24} color={error ? colors.negative : colors.muted} /><Text style={styles.stateText}>{text}</Text></View>; }
const short = (value: string) => value.length > 12 ? `${value.slice(0, 6)}…${value.slice(-4)}` : value;

const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, content: { padding: spacing.lg, paddingBottom: 100 }, header: { flexDirection: "row", alignItems: "center", gap: spacing.md }, heroIcon: { width: 48, height: 48, borderRadius: 16, alignItems: "center", justifyContent: "center", backgroundColor: colors.accentDim, borderWidth: 1, borderColor: colors.accent }, flex: { flex: 1 }, eyebrow: { color: colors.accent, fontSize: 7, fontWeight: "900", letterSpacing: 1.1 }, title: { color: colors.text, fontSize: 28, fontWeight: "900" }, subtitle: { color: colors.muted, fontSize: 9, marginTop: 2 }, status: { alignItems: "center", gap: 4 }, dot: { width: 7, height: 7, borderRadius: 4, backgroundColor: colors.positive }, stale: { backgroundColor: colors.warning }, statusText: { color: colors.muted, fontSize: 7, fontWeight: "800" }, searchRow: { flexDirection: "row", gap: spacing.sm, marginTop: spacing.lg }, searchBox: { flex: 1, minHeight: 42, flexDirection: "row", alignItems: "center", gap: spacing.sm, paddingHorizontal: spacing.md, borderRadius: 12, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, searchInput: { flex: 1, color: colors.text, fontSize: 10, paddingVertical: 0 }, chain: { minWidth: 72, minHeight: 42, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 6, paddingHorizontal: spacing.md, borderRadius: 12, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, chainMark: { color: "#8b5cf6", fontSize: 18, fontWeight: "900" }, chainText: { color: colors.text, fontSize: 9, fontWeight: "900" }, justInHead: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", marginTop: spacing.xl, marginBottom: spacing.sm }, justInTitle: { color: colors.accent, fontSize: 9, fontWeight: "900", letterSpacing: 1.4 }, justInHint: { color: colors.muted, fontSize: 8, marginTop: 3 }, pulse: { flexDirection: "row", alignItems: "center", gap: 5 }, pulseText: { color: colors.muted, fontSize: 7, fontWeight: "800" }, justInRow: { gap: spacing.sm, paddingRight: spacing.lg }, justInCard: { width: 176, padding: spacing.md, borderRadius: 12, borderWidth: 1, borderColor: "#3c2b59", backgroundColor: "#151022" }, justInSymbol: { color: colors.text, fontSize: 10, fontWeight: "900" }, justInType: { fontSize: 7, fontWeight: "900", textTransform: "uppercase" }, justInAmount: { color: colors.text, fontSize: 15, fontWeight: "900", marginTop: spacing.sm }, justInTime: { color: "#a88cff", fontSize: 7, marginTop: spacing.sm }, kpis: { flexDirection: "row", gap: spacing.sm, marginTop: spacing.md }, kpi: { flex: 1, padding: spacing.md, borderRadius: 10, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, kpiLabel: { color: colors.muted, fontSize: 7, textTransform: "uppercase" }, kpiValue: { color: colors.text, fontSize: 11, fontWeight: "900", marginTop: 4 }, good: { color: colors.positive }, bad: { color: colors.negative }, pulsePanel: { padding: spacing.md, marginTop: spacing.sm, borderRadius: 12, borderWidth: 1, borderColor: "#3c2b59", backgroundColor: "#151022" }, pulsePanelTitle: { color: colors.text, fontSize: 10, fontWeight: "900" }, pulsePanelHint: { color: colors.muted, fontSize: 7, marginTop: 3 }, pulseBias: { fontSize: 9, fontWeight: "900" }, pulseMetrics: { flexDirection: "row", gap: spacing.sm, marginTop: spacing.md }, pulseLargest: { color: "#c4a7ff", fontSize: 8, fontWeight: "800", marginTop: spacing.md }, tabs: { flexDirection: "row", marginVertical: spacing.lg, gap: 5 }, tab: { flex: 1, minHeight: 42, alignItems: "center", justifyContent: "center", borderRadius: 10, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, tabActive: { backgroundColor: colors.accentDim, borderColor: colors.accent }, tabText: { color: colors.muted, fontSize: 7, fontWeight: "800" }, tabTextActive: { color: colors.accent }, boundary: { color: colors.warning, fontSize: 8, lineHeight: 13, marginBottom: spacing.md }, controls: { padding: spacing.sm, marginBottom: spacing.md, borderRadius: 12, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, controlsTitle: { color: colors.muted, fontSize: 7, fontWeight: "800", textTransform: "uppercase", margin: 4 }, resetText: { color: colors.accent, fontSize: 8, fontWeight: "900", padding: 4 }, controlRow: { alignItems: "center", gap: 5, paddingTop: 4 }, control: { minHeight: 34, justifyContent: "center", paddingHorizontal: 10, borderRadius: 8, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.background }, controlActive: { borderColor: "#8b5cf6", backgroundColor: "#2d1950" }, controlText: { color: colors.muted, fontSize: 7, fontWeight: "800" }, controlTextActive: { color: "#c4a7ff" }, controlDivider: { width: 1, height: 22, backgroundColor: colors.border, marginHorizontal: 2 }, card: { flexDirection: "row", alignItems: "center", gap: spacing.md, padding: spacing.md, marginBottom: spacing.sm, borderRadius: 12, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, direction: { width: 38, height: 38, borderRadius: 12, alignItems: "center", justifyContent: "center" }, row: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: spacing.sm }, cardTitle: { color: colors.text, fontSize: 11, fontWeight: "900" }, actionText: { fontSize: 9, fontWeight: "900", marginTop: 3, textTransform: "capitalize" }, walletText: { color: colors.text, fontSize: 8, fontWeight: "800", marginTop: 3 }, eventContext: { maxWidth: "46%", alignItems: "flex-end" }, amount: { fontSize: 10, fontWeight: "900" }, meta: { color: colors.muted, fontSize: 8, marginTop: 4 }, evidence: { color: colors.muted, fontSize: 7, marginTop: 5 }, flowCard: { padding: spacing.lg, marginBottom: spacing.sm, borderRadius: 12, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, rankMini: { width: 28, height: 28, borderRadius: 8, alignItems: "center", justifyContent: "center", backgroundColor: "#2d1950" }, rankMiniText: { color: "#c4a7ff", fontSize: 9, fontWeight: "900" }, metrics: { flexDirection: "row", gap: spacing.sm, marginTop: spacing.md }, metric: { flex: 1 }, metricLabel: { color: colors.muted, fontSize: 7 }, metricValue: { color: colors.text, fontSize: 9, fontWeight: "800", marginTop: 3 }, rank: { width: 34, height: 34, borderRadius: 17, alignItems: "center", justifyContent: "center", backgroundColor: colors.accentDim }, rankText: { color: colors.accent, fontWeight: "900" }, primary: { minHeight: 44, alignItems: "center", justifyContent: "center", paddingHorizontal: spacing.lg, borderRadius: 10, backgroundColor: colors.accent, marginTop: spacing.md }, primaryText: { color: colors.background, fontSize: 9, fontWeight: "900" }, secondary: { minHeight: 40, alignItems: "center", justifyContent: "center", paddingHorizontal: spacing.lg, borderRadius: 10, borderWidth: 1, borderColor: colors.accent }, secondaryText: { color: colors.accent, fontSize: 9, fontWeight: "900" }, notice: { alignItems: "center", padding: spacing.xl, borderRadius: 14, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, noticeTitle: { color: colors.text, fontSize: 14, fontWeight: "900", marginTop: spacing.md }, noticeText: { color: colors.muted, fontSize: 9, lineHeight: 15, textAlign: "center", marginTop: spacing.sm }, state: { minHeight: 220, alignItems: "center", justifyContent: "center", gap: spacing.md }, stateText: { color: colors.muted, textAlign: "center", lineHeight: 18 } });
