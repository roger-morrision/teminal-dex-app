import { useRouter } from 'expo-router';
import { Pressable, ScrollView, StyleSheet, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { colors, spacing } from '@/theme';
import { useSettings } from '@/settings/SettingsProvider';
const groups = ['AI Intelligence', 'Track', 'CopyTrade', 'Top Traders', 'Smart Money', 'Wallet Tracker', 'Signals', 'Heatmap', 'Snipe List', 'Analytics', 'Multicharts', 'Feed Data', 'Claim Monitor'];
export default function MoreScreen() { const router = useRouter(); const { t } = useSettings(); return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Text style={styles.eyebrow}>TERMINAL DEX</Text><Text style={styles.title}>{t('moreTools')}</Text><Text style={styles.note}>Routes activate only after their real contracts and security checks are implemented.</Text><Pressable accessibilityRole="button" accessibilityLabel="Open Settings" onPress={() => router.push('/settings')} style={styles.settings}><Text style={styles.label}>{t('settings')}</Text><Text style={[styles.status, styles.live]}>PRIVACY · A11Y · VI/EN</Text></Pressable><Text style={styles.section}>INTELLIGENCE & AUTOMATION</Text><Text style={styles.warning}>CopyTrade saves paused strategies. AI recommendations remain advisory and paper-only.</Text>{groups.map((item) => { const live = ['AI Intelligence', 'CopyTrade', 'Top Traders'].includes(item); const route = item === 'AI Intelligence' ? '/ai' : '/copytrade'; return <Pressable accessibilityRole="button" accessibilityLabel={`${item} ${live ? 'available' : 'queued'}`} key={item} disabled={!live} onPress={() => router.push(route)} style={[styles.row, !live && styles.disabled]}><Text style={styles.label}>{item}</Text><Text style={[styles.status, live && styles.live]}>{live ? 'OPEN' : 'QUEUED'}</Text></Pressable>; })}</ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, content: { padding: spacing.lg, paddingBottom: 90 }, eyebrow: { color: colors.accent, fontSize: 10, fontWeight: '900', letterSpacing: 2 }, title: { color: colors.text, fontSize: 28, fontWeight: '900', marginTop: 4 }, note: { color: colors.muted, lineHeight: 20, marginVertical: spacing.lg }, settings: { minHeight: 58, paddingHorizontal: spacing.lg, marginBottom: spacing.md, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', borderWidth: 1, borderColor: colors.accent, borderRadius: 12, backgroundColor: colors.accentDim }, section: { color: colors.muted, fontSize: 8, fontWeight: '900', letterSpacing: 1.5, marginTop: spacing.md }, warning: { color: colors.warning, fontSize: 9, lineHeight: 14, marginVertical: spacing.md }, row: { minHeight: 54, marginBottom: 2, borderWidth: 1, borderColor: colors.border, borderRadius: 11, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: spacing.lg, backgroundColor: colors.surface }, disabled: { opacity: 0.58 }, label: { color: colors.text, fontWeight: '700' }, status: { color: colors.muted, fontSize: 9, fontWeight: '900', letterSpacing: 1 }, live: { color: colors.accent } });
